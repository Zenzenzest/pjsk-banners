import { useState } from "react";

type Server = "jp" | "global";

export default function HomeContainer() {
  const [server, setServer] = useState<Server>("global");
  const formulas: string[] = ["linear_interpolation", "lagrange_interpolation"]

  const powerCalculation = (character: 'string') => {
    
    switch(attribute)
  }
  return;
}
